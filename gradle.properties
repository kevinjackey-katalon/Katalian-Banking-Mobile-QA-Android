org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official

# Optional: supply your Gemini API key to enable the live "Ask AI Assistant"
# feature (mirrors the web app's GoogleGenAI integration). Leave blank to run
# with the built-in offline fallback responses.
GEMINI_API_KEY=
